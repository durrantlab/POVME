# To run the POVME example, type the following into the unix command line from
# the current directory:

python ../../../POVME2.py sample_POVME_input.ini

# A similar command should work from the Windows command line.
